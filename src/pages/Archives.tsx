
import React from 'react';
import AddButton from 'components/AddButton';

const Archive: React.FC = () => {
    return (
        <div>
            <h1>fArchive</h1>
           {/* <AddButton title={'ajouter'}/>*/}
           <AddButton title={'ajouter utilisateur'} />
        </div>
    );
};

export default Archive;